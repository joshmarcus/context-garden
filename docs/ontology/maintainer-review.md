## Verdict: request changes

Blocking findings:

- **Worker, host, and lease** — The ontology omits the workload-identity model introduced by the reviewed commit: trusted provider/reference/boundary policy, ephemeral resolved authority, public metadata, redaction, renewal/revocation behavior, and environment-failure classification. This leaves a material authority, lifecycle, retention, and secret-handling gap. Add a dedicated section grounded in `workload_identity.py`, `run_supervisor.py`, `remote_worker.py`, and the worker protocol.

- **System-boundary ER diagram** — `REVIEW ||--|| DECISION` asserts a mandatory one-to-one relationship, contradicting the text’s statement that no universal `Decision` entity exists and that decisions are chiefly side-state/events. Remove that entity/edge or model decisions as optional contextual records rather than a mandatory review conclusion.

The remaining documented discrepancies are generally reported candidly rather than normalized away, and the Mermaid syntax appears readable.

ONTOLOGY_REVIEW: {"verdict":"request_changes","reviewed_head":"bcc8419c2dde10ff022621a8a73d37bebdb8d1bc","summary":"The ontology misses the commit's workload-identity authority model and contains a contradictory mandatory Review–Decision cardinality.","findings":[{"severity":"blocking","location":"Worker, host, and lease; Compatibility and extension rules","problem":"The reviewed commit adds workload_identity.py and protocol behavior, but the ontology does not define trusted identity references/providers/boundaries, ephemeral resolved authority, public metadata versus secret values, delivery boundaries, renewal/revocation, redaction, persisted metadata/error receipts, or environment-failure/attempt semantics.","correction":"Add a workload-identity section citing workload_identity.py, run_supervisor.py, remote_worker.py, managed_worker.py, config.py, and docs/worker-protocol.md. Define configuration authority, identity/request metadata, secret non-persistence, bounded delivery, closure/renewal/revocation, redaction, and workload-identity failures as environment failures that do not become author revisions or ambient-credential fallback."},{"severity":"blocking","location":"System boundary and authority ER diagram; Review, finding, and decision","problem":"The Mermaid edge REVIEW ||--|| DECISION requires exactly one Decision per Review and exactly one Review per Decision, while the prose says Decision has no universal class and is represented chiefly by task side state and events; many reviews have no human decision.","correction":"Remove Decision from the ER diagram or replace it with an optional contextual decision-record relationship whose cardinalities match the side-state/event implementation."}]}