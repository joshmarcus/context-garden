## Verdict: request changes

Blocking errors:

- **Run and review identity/cardinality:** Phase persona reviews have no task or PR; they use a synthetic run task ID. The ER diagram and Review section incorrectly require every Review/Run to belong to a Task and PR.
- **Run lifecycle incomplete:** Actual run statuses also include `waiting` and `env_error`; both are omitted from the asserted exhaustive status list. `env_error` is also excluded from archival terminal statuses.
- **ER diagram contradicts the prose and implementation:** It requires each event to concern exactly one Task, each Run to have exactly one Harness and Model, and gives Run zero-to-many Usage records. Events may have an empty task; runs may have empty harness/model; usage is one open mapping per run.
- **Workload-authority cardinality invented:** `WorkloadIdentityResolver.resolve()` has no per-run uniqueness cache or guard. The claim that resolved authority is “at-most-one-per-run” is not implemented.

The Mermaid syntax appears valid, but its semantic cardinalities need correction. The document otherwise appropriately preserves several known discrepancies rather than reconciling them away.

ONTOLOGY_REVIEW: {"verdict":"request_changes","reviewed_head":"bcc8419c2dde10ff022621a8a73d37bebdb8d1bc","summary":"The ontology has blocking identity, cardinality, and lifecycle inaccuracies despite generally strong source coverage.","findings":[{"severity":"blocking","location":"System boundary and authority / ER diagram; Review, finding, and decision","problem":"The diagram and Review definition require task/PR-bound reviews and task-owned runs, but phase persona reviews are auxiliary runs with synthetic task IDs and no task document or PR.","correction":"Model task-PR reviews separately from phase persona reviews; define auxiliary run identity explicitly and remove the universal Task-to-Review cardinality."},{"severity":"blocking","location":"Run and attempt","problem":"The asserted exhaustive run status list omits implemented statuses waiting and env_error; env_error is also not archive-terminal.","correction":"Add waiting and env_error with their lifecycle/retry/retention semantics, and distinguish archival eligibility from lifecycle completion."},{"severity":"blocking","location":"System boundary and authority / ER diagram","problem":"The ER cardinalities conflict with implementation: events can lack task IDs, runs can lack harness/model values, and Run.usage is one mapping rather than zero-to-many Usage entities.","correction":"Correct optionality and model Usage as a single optional/open value mapping, or explicitly introduce persisted entities only where they exist."},{"severity":"blocking","location":"Workload identity and authority","problem":"The document asserts resolved authority is at-most-one-per-run, but WorkloadIdentityResolver.resolve has no per-run uniqueness enforcement.","correction":"Remove the guarantee or implement and document a durable/in-process per-run operation guard."}]}