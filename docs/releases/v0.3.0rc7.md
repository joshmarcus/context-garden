# v0.3.0rc7 validation-policy preview

This candidate adds two isolated owner-requested policy fixes to verifiedrc6: optional trusted capture-infrastructure advisory handling, and120spertest/900shardvalidation/check deadlines with safe owned-descendant cleanup. It preserves original failed capture records and keeps real UI/application/functional/source failures blocking. Ordinary model runs retain their task timeout.

Exact release CI and both changes' current-head review are required before publication or deployment. No storage migration or state reset is intended. Allrc6 limitations remain; this candidate does not include every later main merge or claimPhase05 stabilization. Rollback is verifiedrc6/8f050073, through an accounted runtime boundary preserving all state/results.
