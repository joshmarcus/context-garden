# v0.3.0rc3 — production worker rollout corrections

Preview candidate based on installed v0.3.0rc2, adding only the reviewed CG420/PR302 fixes.

Remote claims resolve configured repository URLs through the controller checkout before reading the publication lease head. The bootstrap installs the complete SHA256-verified official Codex0.153.4 package, including the Code Mode host and vendor resources, with permissions usable by the unprivileged worker.

CG420 passed45 focused tests and lint on the managed AWS host, both full exact-head CI runs, and an isolated replay of the final installer with the real vendor package. A real authenticated worker model job executed tools, passed12 managed-worker tests and returned durable artifacts after the initial live runtime repair. This is supervised recovery evidence; a full clean-image replay of the final bootstrap and the phase05 stabilization gate remain unproven.

No data migration or scheduling-policy change is included. Unrelated merged main changes remain excluded. The new runtime documentation is placed in its own file because the installed release does not contain the broader EC2 environment document. Publish only after exact candidate CI passes, and deploy separately at an accounted boundary. Roll back to v0.3.0rc2/be22506bd00cce32fb0f3acba121a764bafb506c through the versioned deployment protocol, preserving task state and run artifacts.
