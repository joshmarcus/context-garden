# v0.3.0rc6 preview

This candidate packages the merged main snapshot at `7ca5765ff66eb18c3d4372aaed0f898f0d4df58a` plus a focused notification-recipient fence correction and the matching package version.

## Included behavior

- Reuse valid task-specific interaction evidence, select declared affected-flow replays, and retain bounded verification recovery instead of repeatedly revising unchanged implementation.
- Run controller-owned replay checks on the controller; measure admission waiting from the new run and complete local stdin-consuming workers without the prior completion hang.
- Recover harness-parked tasks, accept verified no-change revision outcomes, finish manual work from an exact pushed source, and respect external branch-stack ownership.
- Include merged Now-page unification, request-scoped Store reads, runtime profiling, retrospective/operator accounting, host-identity redaction, and the enterprise notification profile.
- Hold notification-recipient changes against the dispatch fence across controller restart until the owner accepts the reload or the run is reconciled. The enterprise example explicitly requires external protected-branch approval and deployment authorization.

The source snapshot includes every merged commit on main at the cutoff. PR322 merged into its stacked parent rather than main and is excluded. Unmerged CG395 Enterprise-host work is excluded.

## Validation and limitations

The release's attached manifest identifies its exact commit, passing validation commands and CI runs, source selection and operator review disposition. A merged task is not described as independently approved merely because it is included in this owner-selected preview.

Phase05 stabilization is not yet established. Generic replay coverage cannot stand in for a different affected flow. Scope-aware handling of unrelated limitations in an `unverified` list remains follow-up CG443. The wider September8 WSL/web incident cause remains unresolved; this release does not claim it fixed the whole incident. External organization approval and deployment controls must be configured outside the example overlay.

No storage reset, worker credential rotation, host launch, concurrency change, or billing/deadline extension is part of creating this release. The six-host trial still ends at 20:00UTC on September8 unless separately extended by the owner.

## Installation and rollback

Publication does not install this build. Use an accounted deployment boundary to preserve active runs and durable results, install the immutable release commit, and verify the runtime/package version and installed source. Roll back through the same process to v0.3.0rc5 / `41d0da0c6d623a1e2c7c59821b1c7d4f69218577`, preserving state and evidence. Do not deploy withdrawn rc4.

GitHub source archives are the distribution artifacts; no PyPI or wheel publication is claimed.
