# Context Garden 0.3.0rc20

This release candidate repairs failures observed while operating RC19. Historical branch cleanup uses bounded remote-reference discovery. The final-output supervisor handles FIFO and atomic-replacement handoff with bounded shutdown and preserves explicit incomplete-output evidence. Codex structured output remains available for native final-result parsing. Cleanup recovery references now distinguish complete run IDs from their same-second suffixed siblings, fixing the reproduced storage-cleanup CI failure while retaining actual recovery ownership. Local harness startup separates writable runtime state from protected credential inputs, addressing the read-only home that prevented the observed Codex attempt from starting.

The release is built from accepted main commit 52d8ed540995efc108961364c76fc86a7bd7c6c8. Accepted repairs are PR477/source8df02641fec6411278b1f471d2fbd45b3968b090, PR478/source9945f1f2bf2711adfaa62a01c20d6af758671e76, PR480/sourcef3c08b2dfb1e6d1e0b4a1f045eb8b3f1ae1f0d69, and PR482/source69d903ace81528ee4056e663339eb70cd18b0d1a. All are actually merged and have passing exact-source CI. Independent source acceptance preserves its original review run and, where present, the native verified unchanged-patch rebase lineage. Version metadata and these notes are the only changes above that main source.

Initial unpublished RC20 candidate541aa9ac and its passing CI/protocol/182focused/22ingress checks remain preserved. Subsequent local startup failure prevented its publication. The first focused test attempt also loaded an unintended fixture plugin because the temporary tests were placed beneath the protocol test directory; its original failure is retained, and the corrected layout passed without a candidate-source change. Final-source validation and independent release review are required anew; earlier candidate evidence is not substituted for those gates.

RC19's original failures and interventions remain recorded. Cleanup delayed collection and dispatch while authenticated claims, heartbeats and finish continued. Several model-completed runs required guarded childless-supervisor recovery with original source, outputs, interrupted exits and accounting retained. Two local Codex attempts failed before model output because the generated state directory was read-only; their original empty outputs, diagnostics and absent exit/final evidence remain explicit. Passing later validation does not erase those observations.

During preparation, the worker-only ingress was starved by its256MiB component memory.high while using about295MiB. Workers encountered timeouts/502s and automatic daemon restarts; the local-state repair's final full-suite run was interrupted. The operator preserved its original commits and validation output before recovery returned a separate no-output/base-head failure. Fresh source CI and independent review are required for that recovered repair. The endpoint received a512MiB high/768MiB maximum component allowance and an isolated accounted restart, while the aggregate controller2CPU/5GiB cap stayed unchanged. Original incident and recovery receipts are retained.

CG620 tracks a separately reproduced stale-collection continuation gap: a newer worker final is preserved, but a collector can advance while leaving its check marked running after its pointer is cleared. It remains an explicit Phase05 closing blocker at this candidate preparation. The operator must reconcile any existing finished orphan through the supported terminal-check path before claiming a drained rollout. This candidate does not claim that defect is fixed; its source is assessed in the independent release review alongside the requested interim RC scope.

Publication requires passing CI for the exact merged main and release commits, independent source and rollout review, focused deterministic lifecycle tests and an RC19-client TCP journey against the exact candidate. Publication and installation have separate receipts. The planned rollout covers the existing Ubuntu WSL controller and Linux workers; macOS is not exercised by that rollout. No universal page-latency or additional live-cloud scenario claim is made.

Phase05 remains open pending its closing account and owner disposition. This candidate does not establish stable-release readiness or phase closure. The rollout preserves active work, original evidence, rollback, capacity and physical limits, the same six-worker maximum, aggregate80USDcap and authorized September11,2026 11:36:42UTC deadline. Withdrawn Phase09Herdr work is not reactivated.
