# CG-300 served validation

This disposable fixture was served from `docs/design/now2-live/serve_fixture.py` at
`http://127.0.0.1:8769` on commit `a266923c2dbf589fa61d87e110de2e14e34accf6`.
The interaction replay and observations are in `interaction-manifest.json`. The eight
PNG captures cover `/costs` and `/now2` at desktop and verified 390 CSS-pixel iframe
widths, in light and dark mode.
