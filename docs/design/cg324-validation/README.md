# Required evidence validation

A disposable app on port 8782, watch disabled and synthetic tasks/state only, rendered a posted persona review, queued captures and a failed named check distinctly on the task page. Navigated to the task, inspected the required-evidence panel, and read captures at 1280 and 390 CSS pixels in light/dark modes. The harness selects the existing data-theme CSS through a capture-only query parameter. No real model call or GitHub comment was generated for the screenshots.

125 targeted scheduler poll, web, QA and walkthrough tests passed before integration of the operating-UI merge. These exercise required persona-before-review ordering, failed persona handling and retry, evidence state rendering, and actual disposable application flows. Current-head CI is required after the final main integration.
