# CG-337 decision-card validation

The four captures render the disposable Inbox from `serve_fixture.py` at 1280 CSS pixels
and through a measured 390 CSS-pixel iframe, in light and dark mode. They show the
cancellation recommendation, changed-outcome consequences, and missing-question recovery
operation together. The embedded narrow page measured `clientWidth=390` and
`scrollWidth=390`.
